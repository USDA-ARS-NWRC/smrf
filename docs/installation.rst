.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install smrf

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv smrf
    $ pip install smrf
